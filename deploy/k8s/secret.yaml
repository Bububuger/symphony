# Example template — populate with actual values via:
#   kubectl create secret generic symphony-secrets \
#     --from-literal=release-cookie="$(openssl rand -hex 32)" \
#     --from-literal=linear-api-key="lin_api_XXXX"
#
# DO NOT commit real secrets to this file.
apiVersion: v1
kind: Secret
metadata:
  name: symphony-secrets
type: Opaque
stringData:
  release-cookie: "CHANGE_ME_USE_A_RANDOM_STRING"
  linear-api-key: "CHANGE_ME"
